# 验证记录

日期：2026-09-22。环境：Windows、Node.js 24.14、Python 静态服务、Codex 内置桌面浏览器。未连接 WebXR 头显。

## 自动化

- `npm test`：16/16 通过。
- `node --check`：src 和 tools 下 56 个 JavaScript 文件通过。
- `git diff --check`：无空白错误；仅 Git 的 LF/CRLF 提示。
- 独立代码审查发现的视频暂停重试问题已修复，并添加回归；同时补充退出关卡媒体清理及龙关异步退出回归。

测试覆盖：攻击强化不降级、跨帧选卡碰撞、模型失败占位和重试、共享解析缓存、天空乱序请求和菜单失效、三张天空缓存释放、暂停原因叠加、设置校验、失焦输入清理、满级卡池补给、左手模式重连、工具栏焦点下 P 暂停、视频 AbortError 不重试、退出后旧语音不恢复、龙关加载完成后不生成已弃用部件。

测试中的 simulated 404 是主动模拟资源失败，预期会输出警告，不代表测试失败。

## 浏览器检查

- 启动进度完成后可进入菜单和第一关。
- 第一关天空、传送门、敌人、地毯正常显示。
- 暂停期间倒计时保持；设置可保存、重新打开，继续按钮恢复玩法。
- 第 6、12、18 关快捷入口可切换，无捕获到的控制台 error/warn；这里只做加载冒烟，未宣称完成三关战斗。
- 使用新本地端口避开旧模块缓存后，再次确认第 12 关龙头/龙身显示、暂停面板及调整后的 HUD。
- 深度立绘烘焙工具成功生成 9 套图集与 manifest，共 18 张 PNG，约 18.3 MiB。
- 桌面轻载观察约 60 FPS、帧间隔 P95 约 17ms。此数据仅证明该浏览器场景可运行，不能推断头显性能提升比例。

## 待真机验收

- [ ] PICO 与 Quest：受信任 HTTPS 下进入、退出、重新进入 immersive-vr；local-floor 不可用时回退。
- [ ] 左右手重连、惯用手切换、右摇杆移动、左摇杆三种转向模式及坐姿补偿。
- [ ] A/B 暂停菜单、系统菜单/摘戴可见性变化；恢复前松扳机；视频与语音同步暂停。
- [ ] 从第 1 关连续游玩至第 18 关，包含选卡、技能、死亡重开及 Boss 击杀。
- [ ] 2K/4K 天空下持续游玩、反复切关，记录纹理数及设备内存变化。
- [ ] 分别在 72Hz（13.89ms）和 90Hz（11.11ms）目标下导出性能 JSON，结合设备 GPU 工具检查超预算原因。
- [ ] 慢网/断网时启动重试和简化模式；龙关关键动画资源失败时仍可通过暂停返回菜单或重新选关。

## 已知边界

设置面板为桌面 DOM，沉浸式内只提供 3D 暂停提示及 A/B 操作。天空 2K 设置刷新生效，不减少原图下载流量。共享模型和立绘仍使用跨关缓存；天空三张容量不等于整个应用纹理总量上限。尚未提供自动通关测试或真机 GPU 基准。

2026-09-22 入口调整：取消启动资源遮罩和 assetsReady 门槛，开始按钮直接开局，资源后台预热。16 项回归测试仍通过，main.js 语法检查通过；此前关于启动进度/重试页面的记录仅对应调整前版本。

## 第 6 关技能连锁击杀修复

根据头显截图中的 `_updateBuddhaFx` / `reading mesh` 报错定位：击杀脸谱子实体会递归击杀并移除 Boss，导致原地倒序遍历敌人数组时索引越界。召唤者死亡清理多个小怪、激光剑伤害遍历存在同类问题。

神掌和激光剑改为遍历当前敌人快照，并跳过已经死亡或移出管理器的对象，避免越界、重复计分及对已释放对象再次结算。新增 4 项回归，使用实际 Game 击杀结算与 BalloonManager 移除逻辑，覆盖两种技能与两种连锁移除场景。修复前均复现异常，修复后全部通过；总计 20/20 测试通过，game.js 语法与 diff 空白检查通过。尚未在用户头显上复测。

## VR 设备晚连接检测修复（2026-09-22）

原入口只探测一次，返回 false 后永久禁用按钮。现在监听 WebXR devicechange、窗口 focus 和页面恢复可见事件重新探测；检测为 false 或抛错时保留“重试进入 VR”，缺少 WebXR API 时显示独立提示。点击直接请求 VR 会话，自动探测不会启动会话。探测结果使用版本号并检查会话状态，避免旧结果覆盖新结果或在启动期间解锁按钮。

新增 5 项测试覆盖晚连接恢复、运行时检测异常后恢复、乱序结果、会话启动期间的旧结果和无 WebXR API。新增测试先失败后通过，总计 25/25 测试通过，main.js 与 availability.js 语法检查通过。需要刷新页面加载修复；尚未完成 PICO 真机复测，此修复不代表此前 SteamVR SharedTexturesNotSupported 错误已解决。
