# WebXR 打气球 · 技术文档索引

> 本文档集面向「从零复现 / 移植」场景：把本项目里**无法一次做对、必须反复调试**的关键技术，
> 按主题拆成独立文档。每篇统一结构：**目标 → 原理 → 关键实现 → 参数表 → 踩坑（多次调试点） → 验证方法**。

---

## 1. 项目概况

| 项 | 值 |
|---|---|
| 形态 | WebXR VR 射击肉鸽（打气球），纯静态站点 |
| 目标设备 | PICO 4（SoC：Adreno XR2，浏览器内核 Chrome 105） |
| 渲染 | Three.js r168，`importmap` 把 `three` 指到本地 `vendor/three.module.js` |
| 工程形态 | **纯静态 ES Modules，无构建步骤、无 node_modules**（`vendor/` 本地化依赖） |
| 输入 | 双 6DoF 手柄（WebXR `xr-standard`）；桌面回退 WASD + 鼠标 |
| 性能预算 | 72 fps（头显），因此纹理/分辨率/胶水层都按此裁剪 |
| 关卡 | 18 关：普通(1/4/7/10/13/16) / 危机(2/5/8/11/14/17) / Boss(6/12/18) / 激光机制(3/9/15) |

**为什么这份文档值得单独存在**：本项目几乎每个子系统都踩过「看起来能跑、实机必翻车」的坑
（控制器左右手、PICO 摇杆轴向、EXR 色彩空间、离屏捕获空图、视频解码×编码争用……），
这些结论都是实机实测得到的，照抄代码不等于能跑对。

---

## 2. 文档清单

| # | 文档 | 主题 | 主要源码 | 复现难度 |
|---|---|---|---|---|
| 01 | [VR 输入实现（移动射击）](./01-VR输入实现（移动射击）.md) | 双端输入抽象、控制器绑定、移动与瞄准射击 | `src/vr/input.js` | ★★★ |
| 02 | [调试日志（布局与实现）](./02-调试日志（布局与实现）.md) | 页面日志面板 + 头显内腕表面板 | `src/ui/pagelog.js`、`src/vr/wrist-ui.js`、`src/game/game.js` | ★★ |
| 03 | [模型优化（GLB+Draco 与 2.5D 立绘）](./03-模型优化（GLB与Draco压缩·2.5D立绘）.md) | 资源加载/缓存/克隆，GLB→深度视差立绘 | `glbCache.js`、`glbCapture.js`、`depthSprite.js` | ★★★★ |
| 04 | [天空盒实现](./04-天空盒实现.md) | 渐变天球 + 全景穹顶、色彩空间与各向异性 | `src/core/world.js` | ★★★ |
| 05 | [游戏打包方式（EXE + APK）](./05-游戏打包方式（EXE与APK）.md) | PC 直播 EXE 与头显 APK 的构建与重打包边界 | `tools/cast-pc/`、`tools/cast-apk/` | ★★★ |
| 06 | [游戏内视频播放技术](./06-游戏内视频播放技术.md) | 沉浸态下播放影片、自动播放解锁、三重兜底 | `src/game/waitingRoom.js` | ★★★★ |
| 07 | [VR+ 平台版本号实现](./VR+平台版本号实现.md) | 平台按包名匹配版本号；`applicationId` 必须与平台登记一致 | `tools/cast-apk/app/build.gradle` | ★ |
| 08 | [授权服务器与 License 方案](./08-授权服务器与License方案.md) | 商业化授权：服务器私钥签发 license + EXE 持有证明（PoP）；**服务器 P1 已实现**（`tools/cast-server/`，自测 36/36，`deploy/install.sh` 一键部署），EXE / 头显接入待做 | `tools/cast-server/`、`tools/cast-pc/main.js`、`MainActivity.java` | ★★★★；**2026-09-24 起默认关闭**（第二十三修，`--license` 恢复），配置改由 09 的「服务器清单」下发 |
| 09 | [服务器清单与旧配置清理](./09-服务器清单与旧配置清理.md) | **配置不再随包走**：每次启动从服务器拉清单，关闭游戏 / 下次启动前清掉旧配置；授权校验已降级为 `--license` 可选 | `tools/cast-server/server.js`、`sync-content.js`、`tools/cast-pc/main.js`、`MainActivity.java` | ★★★ |

> 另有 3 份「专项」文档（位于 `docs/`，与本文档集互相引用，不重复）：
> `docs/cast-architecture.md`（直播根因与踩坑史）、`docs/cast-implementation-and-packaging.md`（实现与打包复现）、
> [`docs/内容授权门禁设计.md`](../内容授权门禁设计.md)（**08 的续篇**：把关卡数据从「随安装包分发」改成
> 「每次启动向授权服务器换取」—— 含 `POST /api/content/grant` 协议、逐文件改动清单与行号、
> 部署规范、安全边界与验证项。**设计定稿，待实现**）。
>
> **跨项目通用手册（自包含，可交付给别的项目直接复用）**：
> - [`docs/WebXR黑屏排错手册.md`](../WebXR黑屏排错手册.md) —— 「网页打得开、有声音、就是黑屏」的分层排查树
>   （沉浸侧 / 镜像侧 / 环境层三棵树 + 取证手段 + 失败尝试黑名单 + 上线检查清单）。
> - [`docs/headset-video-playback.md`](../headset-video-playback.md) —— 沉浸态播视频的知识点与避坑手册。

---

## 3. 模块依赖关系

```
constants.js（全部可调参数：MOVE / SHOOT / SKY_* / DEPTH_SPRITE_* / WRIST_UI / WAITING_ROOM / CAST …）
   │
   ├─ 01 VR 输入 ── input.js ──► world.camera / world.renderer.xr / rig
   ├─ 02 调试日志 ─ pagelog.js（经典脚本，独立）
   │                wrist-ui.js ──► 挂在 input 的 grip 上
   ├─ 03 模型优化 ─ glbCache.js ──► GLTFLoader + DRACOLoader(vendor/draco)
   │                glbCapture.js ──► depthSprite.js（GLSL3 材质）
   ├─ 04 天空盒 ─── world.js（渐变 ShaderMaterial + 全景穹顶 + EXRLoader）
   ├─ 05 打包 ───── tools/cast-pc（Electron）/ tools/cast-apk（Gradle + NanoHTTPD）
   ├─ 06 视频 ───── waitingRoom.js ──► VideoTexture / CanvasTexture ──► world.setAmbientVisible
   ├─ 08 授权 ───── tools/cast-server（license 签发）/ tools/cast-pc（licenseGate，**默认关闭**）
   └─ 09 清单 ───── sync-content.js ──► /api/manifest ──► tools/cast-pc userData/manifest ──► 头显 overlay
```

---

## 4. 建议阅读顺序

1. **先读 01**：输入是所有交互的入口，理解 `rig / camera / controller` 三级关系后，其余模块更好定位。
2. **再读 02**：调试日志是后续所有实机调试的工具，先布好「眼睛」。
3. **03 / 04 可并行**：一个管「模型怎么进场景」，一个管「场景长什么样」。
4. **06** 依赖 05 的打包形态（影片阶段要通知 PC 端），建议在打包之后读。
5. **05 最后实操**：打包是发布动作，代码稳定后再做。

---

## 5. 「必须多次调试」的清单（各文档都收录，此处汇总）

| 主题 | 一次做对会翻车的点 |
|---|---|
| VR 输入 | 控制器左右手**不能按索引**（PICO 不保证 0=左）；PICO 摇杆在 `axes[2]/[3]` 且前推为负；瞄准方向是**本地 −Z**（`getWorldDirection` 给的是 +Z=背后）；控制器要挂 `rig` 而非 `camera`；按键必须做**边缘检测** |
| 调试日志 | 日志脚本必须是**经典脚本（非 module）**才能早于业务模块加载并捕获加载期报错；沉浸态 DOM 不可见 → 必须有头显内面板 |
| 模型优化 | Draco 解码器要**离线本地**；克隆必须重绑骨骼；`fitToRadius` 必须**先缩放再居中**；离屏捕获要判空并回退；深度图是**数据**不能 sRGB 解码 |
| 天空盒 | **不能**用 `scene.background` 的 equirect（绕过各向异性 → 糊）；EXR 走线性（`NoColorSpace`）；穹顶要转 yaw 对齐正前；各向异性钳到 4 |
| 打包 | EXE 的 `asar:true` → 改渲染层必须重出 EXE；APK 的 assets 是**拷贝**，改 JS 后要重跑同步；Gradle/JDK 版本敏感 |
| 视频播放 | 沉浸态 DOM 不可见 → 必须纹理化；`<video>` 只能**一个消费者**；自动播放被拦要静音重试；**解码 × 编码争 VPU** → 影片期间不推流；必须三重兜底防软锁 |
| 授权 / License | 「生成密钥」≠「签发凭据」；静态凭据可被拷贝转发（需 PoP 绑实例私钥）；共享密钥**不可能**藏进「要给头显看」的凭据；Ed25519 在 `minSdk 24` 不可用（选 P-256）；Node 对 ECDSA 默认输出 **DER**（与 Java 天然对齐，勿设 `ieee-p1363`）；离线兜底常开会架空授权服务器 |

| 服务器清单（第二十四修） | 「**启动无条件先清**」才是主防线 —— 平台关局是 `kill`，`before-quit` 不会执行；清单缓存先落 `.tmp` 再 `rename` **原子替换**；清单不可用时**绝不回落**本地旧文件（应急才 `--local-content`）；`ver` 指定了就必须命中，**不回落 `current`**；头显启动清理要留 `if (!passThrough)` 例外（否则把玩家当场踢出 VR） |

| 平台二次拉起（第二十六修） | 平台的「启动游戏」会在 ~20s 后**重发整套**（含 `kill`）⇒ 头显侧会换进程；「免打扰」判据原先只存在**进程内**（`sLaunched` / `lastPageHitMs`）→ 新进程判据全空 → 建 2D 窗口 → PICO 弹「退出PICO浏览器」并挤掉 XR 会话。修法：**页面在场证据跨进程落盘**（`PagePresence`，6s 内打点 / 90s 内打点且 `xr=1` 都算活）+ 页面在 XR 里时**不发任何前台动作**只 `finish()` + 5s 兜底复核 |

---

## 6. 源码速查（按主题定位）

| 主题 | 文件 |
|---|---|
| 参数总表 | `src/core/constants.js`（+ `src/core/userConfig.js` 玩家可覆盖项） |
| 输入 | `src/vr/input.js` |
| 世界/渲染/天空 | `src/core/world.js` |
| 日志 UI | `src/ui/pagelog.js`、`src/vr/wrist-ui.js` |
| 模型缓存 | `src/game/glbCache.js`、`src/game/balloonModels.js` |
| 2.5D 立绘 | `src/game/depthSprite.js`、`src/game/glbCapture.js` |
| 影片 | `src/game/waitingRoom.js` |
| 直播 | `src/net/cast.js` |
| 打包 | `tools/cast-pc/`、`tools/cast-apk/` |
| VR+ 版本号 | `tools/cast-apk/app/build.gradle`（`applicationId` 必须与平台登记 `com.GoodNet.DeepmindHacker` 一致） |
| 改名 / 改版本（发行身份） | `tools/rebrand.ps1`（一键改 `applicationId` / `app_name` / `versionName` / `versionCode`，可选 PC 端 `appId` / `productName`；含打包 + `aapt2` 产物身份自证 + 交付目录同步）、`tools/rename-game.bat`（双击问答入口）。**改完必须让平台方登记新包名**，且头显上变成全新应用、要重新授予悬浮窗权限；详见 `docs/tech/05-游戏打包方式（EXE与APK）.md` 末节 |
| 授权 / License | `docs/tech/08-授权服务器与License方案.md`、`docs/内容授权门禁设计.md`（内容门禁：唯一内容源 + 签名 + 绑机）；`tools/cast-server/`（P1 已实现：`server.js` / `lib/license.js` / `gen-keys.js` / `selftest.js` / `deploy/install.sh` 一键部署 / `xlang/` 跨语言验签）；`tools/cast-pc/main.js`（License 段 + 内容门禁段，后者**待实现**）、`MainActivity.java`（`guardSecret()` / `fetchExeLicense()`） |
| 配置清单（第二十四修） | `tools/cast-server/server.js`（`/api/manifest`）、`tools/cast-server/sync-content.js`（发布）、`tools/cast-pc/main.js`（「服务器清单 · 运行态」段）、`MainActivity.java`（`clearOverlayCache`）、`docs/tech/09-服务器清单与旧配置清理.md` |
| 现场取证 · 一局采集（2026-09-24） | `tools/collect-round.bat`（双击入口，自动弹 UAC 提权）+ `tools/cap-round.py`（抓游戏通道 UDP 51124/51234/62135/62136 原始帧 + 平台 `DebugLog` → 一页报告）：**已用它结案：平台的「游戏结束」是 CMD 7 `GameStatistics`（CMD 6 从未出现），平台收到即弹结算、且不关游戏**；用法与「必须自然打完、别点平台的结束游戏」铁律、CMD 7 字节样本见 `docs/tech/05-游戏打包方式（EXE与APK）.md` 末节 |
| 现场轻量验证 · 不发一整局（2026-09-24） | `tools\trigger-game-result.bat`（双击入口；自动扫 8443~8453 找 PC 端、先展示 `gameChannel.enabled` / `round.armed` 前提、再发一次 CMD 7）：用来验「平台收到 CMD 7 会不会弹结算、会不会顺手关游戏」；`-DryRun` 只看状态，`-Port` 跳过探测。配套 `POST /api/platform/game-result` 端点与排障口径见 `docs/tech/05-游戏打包方式（EXE与APK）.md` 末节；⚠ **首次现场「发出去了、平台没反应」= 上行帧源 IP 是回环（`127.0.0.1`）被平台按「外来 IP」丢弃**，已由 `usableTarget()` / `platformTargets()` 过滤修好（第三十一修，详见该末节〈⚠ 第一次跑这个脚本没反应？先看源 IP〉） |
| 第二场起不来 · 页面在场记录（第二十九修） | 第二十六修的「90s 内有打点且 `xr=1` 也算活着」在**本局已结束**时变成毒药：第一场结束（页面在 XR 里被杀）后记录永远带 `xr=true` ⇒ 第二场平台 `am start` 被判成「页面还在 → 不重开浏览器」整局吞掉。修法：`PagePresence.markRoundOver()`（退出策略 / `/api/page/dead` / `DEAD:` 生命周期事件三个触发点；页面再次打点即撤销）、`aliveNow()` 见标记即 false、`ALIVE_XR_MS` 90s→45s、免打扰复核判据收紧为 8s 新鲜度（`MainActivity.PAGE_FRESH_MS`）；纪要见 `docs/cast-architecture.md` 附录 I |
| 平台重拉弹窗 · 包内零入口（第二十八修） | 第二十七修只停了 `MainActivity`，平台于是**退化成隐式 MAIN/LAUNCHER** 命中「恢复入口」（p27 留痕 14:59:17 命中 → 14:59:17.618 `xr-end`）⇒ 本修把不变式收紧为「页面在 XR 会话里 ⇔ 包内 MAIN/LAUNCHER 入口数 = 0」：`EntryLock.holdForXr` 由 `apply(false,true)` 改 `apply(false,false)`、`AndroidManifest.xml`（`RecoverActivity` 停用期间不再启用）、`PageForensics.java`（build 标记 `2026-09-24-p28-zero-entry`）；人工恢复走 `http://<头显IP>:8080/api/entry/unlock`；纪要见 `docs/cast-architecture.md` 附录 H |
| 平台重拉弹窗（第二十七修） | 本局在 VR 中临时停用平台入口（让平台那次 `am start` 在包管理器层被拒）：`tools/cast-apk/app/src/main/java/com/local/webxrcast/EntryLock.java`（新）、`RecoverActivity.java`（新，人工恢复入口）、`EntryFuseReceiver.java` / `BootReceiver.java`（新，死人开关 / 开机恢复）、`AndroidManifest.xml`、`GameServer.java`（`/api/entry/*`）、`MainActivity.java`（配置页开关 + 心跳复核）、`CastApp.java`、`activity_main.xml`；纪要见 `docs/cast-architecture.md` 附录 G（人工恢复入口口径已被第二十八修改写） |
| 平台二次拉起 / 跨进程页面在场（第二十六修） | `tools/cast-apk/app/src/main/java/com/local/webxrcast/PagePresence.java`（新）、`GameServer.java`（`lastPageXr`）、`MainActivity.java`（`onCreate` 免打扰分支 / `onNewIntent` / `schedulePassThroughVerify`）、`src/game/game.js`（`_confirmThenGone` 两轮复核）；纪要见 `docs/cast-architecture.md` 附录 F |
